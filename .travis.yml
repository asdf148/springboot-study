language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: "./gradlew clean build"

  chmod +x gradlew

# CI 실행 완료 시 메일 알람
notifications:
  email:
    recipients:
      - wnduf0405_@naver.com